import { CatalogView } from "@/components/commerce/catalog-view";

export const metadata = { title: "Products — Clothist" };

export default function ProductsPage() {
  return <CatalogView title="Products." eyebrow="03 / Catalog" />;
}
